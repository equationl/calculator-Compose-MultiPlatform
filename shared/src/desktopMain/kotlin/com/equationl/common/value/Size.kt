package com.equationl.common.value

import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

val defaultWindowSize = DpSize(360.dp, 640.dp)
val landWindowSize = DpSize(640.dp, 360.dp)